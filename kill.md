# kill

#TO-DO 

```bash
kill     # send SIGTERM
kill -15 # send SIGTERM
kill -9  #send SIGKILL
kill -2  #send SIGINT (CTRL+C)
kill -20  #send SIGTSTP (CTRL+Z) <- stop process
kill -18  #send SIGCONT similar to ´fg´ or ´bg´ command <- continue stopped process
kill -1  #send SIGHUP


```
